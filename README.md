# SIG-Stakeholder-Database

## Projects in this repo

| Path | Description |
|------|-------------|
| `lusers.php` | Stakeholder user list (requires MySQL via `config.php`) |
| [`meet/`](meet/) | **Meet Scheduler** — availability polling app using plain-text files only (no database) |
